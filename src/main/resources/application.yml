spring:
  application:
    name: SW Train Application
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false
  datasource:
    username: sa
    password: sa
    url: jdbc:h2:mem:db;DB_CLOSE_DELAY=-1
  h2:
    console:
      enabled: true
server:
  port: 8080
